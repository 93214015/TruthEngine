#pragma once


enum class TE_IDX_CONSTANTBUFFER : IDX
{
	NONE,
	UNBOUNDED_PER_MESH,
	PER_FRAME,
	PER_OBJECT,
	PER_MESH,
	PER_DLIGHT,

	DIRECT_CONSTANTS,
	DIRECT_PER_MESH
};

enum class TE_IDX_TEXTURE : IDX
{
	UNBOUNDED_MATERIALTEXTURE_DIFFUSE,
	UNBOUNDED_MATERIALTEXTURE_NORMAL,
	UNBOUNDED_MATERIALTEXTURE_DISPLACEMENT,
	SHADOWMAP,
	BACKBUFFER,
	GBUFFER_COLOR,
	GBUFFER_NORMAL,
	GBUFFER_SPECULAR,
	DEPTHMAP_RENDER3D
};

enum class TE_IDX_RENDERTARGET : IDX
{
	SCENEBUFFER
};

enum class TE_IDX_DEPTHSTENCIL : IDX
{
	SCENEBUFFER
};

enum class TE_IDX_SHADERCLASS : IDX
{
	NONE,
	FORWARDRENDERING,

	TOTALNUM
};

enum class TE_IDX_RENDERPASS : IDX
{
	NONE,
	FORWARDRENDERING
};